@startuml Colaboracao_03_Simplificado

left to right direction

skinparam backgroundColor #FFFFFF
skinparam defaultFontName Arial
skinparam defaultFontSize 11
skinparam padding 8
skinparam nodesep 110
skinparam ranksep 80

skinparam rectangle {
    BackgroundColor #EEF2FF
    BorderColor     #4F46E5
    FontColor       #1E1B4B
    FontStyle       bold
    RoundCorner     10
}

skinparam note {
    BackgroundColor #FFFBEB
    BorderColor     #D97706
    FontColor       #78350F
    FontStyle       bold
    FontSize        10
}

skinparam arrow {
    Color           #374151
    FontColor       #000000
    FontStyle       bold
    FontSize        12
    Thickness       1
}

title
    <b>Colaboração 3 — Ciclo Completo De Missão</b>
end title

rectangle "op : Operador\n─────────────────\nnivelAcesso = COMANDANTE" as OP

rectangle "missao : Missao\n─────────────────\nstatus = AGUARDANDO\ndronesIds = {}\noperadorResponsavelId = null" as MIS

rectangle "cdc : CentralDeControle\n─────────────────\nmissoes\nlogs" as CDC

rectangle "log : LogAuditoria\n─────────────────\nmissaoId = missao.id\nusuarioId = op.id\nacao : TipoAcao" as LOG

OP  -[hidden]right- MIS
MIS -[hidden]right- CDC
CDC -[hidden]right- LOG

' ── Criação ──────────────────────────────────────

OP --> MIS : 1. new Missao()

OP --> CDC : 2. adicionarMissao()

' ── Verificação de permissão ─────────────────────

OP --> OP : 3. temPoderDeDecisao()


' ── Alocação ─────────────────────────────────────

OP --> MIS : 4. alocarDrone()

MIS --> LOG : 4.1. Registrar Log

' ── Definição de responsável ─────────────────────

OP --> MIS : 5. definirOperadorResponsavel()

MIS --> LOG : 5.1. Registrar Log

' ── Início da missão ─────────────────────────────

OP --> MIS : 6. iniciarMissao()

MIS --> MIS : 6.1. status = EM_CURSO

MIS --> LOG : 6.2. ALTERACAO_STATUS

' ── Aborto da missão ─────────────────────────────

OP --> MIS : 7. abortarMissao(motivo)

MIS --> MIS : 7.1. status = ABORTADA

MIS --> LOG : 7.2. ALTERACAO_STATUS


' ── Finalização ──────────────────────────────────

OP --> MIS : 8. finalizarMissao()

MIS --> MIS : 8.1. status = SUCESSO

MIS --> LOG : 8.2. ALTERACAO_STATUS

CDC --> MIS : 9. sincronizarLogsMissao()


@enduml